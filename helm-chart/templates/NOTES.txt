{{- if .Values.service.type }}
1. Get application URL by running these commands:

  Namespace: {{ .Release.Namespace }}
  Service: {{ include "helm-chart.fullname" . }}
  Service Type: {{ .Values.service.type }}
  Port: {{ .Values.service.port }}

  If you are on OpenShift, you can get the route URL:

  Route: 
  {{- if .Values.ingress.enabled }}
  {{ .Release.Name }} -> ingress enabled
  {{- else }}
  # OpenShift route automatically created (sandbox)
  oc get route {{ include "helm-chart.fullname" . }} -n {{ .Release.Namespace }}
  {{- end }}

2. Access the application:

  For HTTP: http://<ROUTE_HOST>:{{ .Values.service.port }}

3. Secrets used:

  DB_USER: from secret "app-secrets"
  DB_PASS: from secret "app-secrets"

4. Blue/Green deployment info:

  Current active color: {{ .Values.activeColor }}

{{- end }}
